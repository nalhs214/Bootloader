todo list (디버깅 순서?)

	MCU 						Linux
						argc, argv로 입력을 받음

						binary path, uart path(MCU와 열린 port)를 받음

						bin_fd로 파일을 오픈함 -> 안되면 error

						열런 파일로 256byte로 나눠 몇번이나 flash해야하는지 확인

						uart_fd로 포트 열기
                                                ㄴ(SIGIO 설정, 기본 UART baudrate, 8bit, parity 등)

UART 열리자마자 'R' 전송		SIGIO로 전송되는 메세지가 'R'인지 확인 -> 안오면 종료

						DTR 리셋 / ioctl로 DTR LOW - 100ms - HIGH

리셋	-> 'R' 전송				리셋 후 부팅되어서 'R' 전송 되는지 확인 -> 안오면 종료

						256 데이터 패킷화
						StartCode(1) MsgType(1) DataLen(2) Data(256) CRC(2) = 262
			
						262byte 전송  int w = write(uart_fd, pkt, pkt_size);

recv_packet(bl_data_packet_t *pkt)
262byte를 일단 전송 받음

START code가 맞는지 확인

CRC 값을 비교해서 데이터 신뢰성 확인

Msg_type가 1이면 데이터이므로
Flash_page_wirte로 app 영역에 플레쉬
ACK 신호 전송
						
						ACK 신호가 오는지 3 사이클동안 확인
						신호가 왔으면 다음 page 전송 ---->  안왔으면 종료

남은 데이터 수신 계속			남은 데이터 읽고, 패킷화, 전송 계속

						data를 다 보냈으면 end 패킷 전송

end 패킷으로 받으면 
MSG_TYPE이 달라서 끝났음을 확인

app 영역 주소를 read 해서 비어있는지 확인
잘 플레싱 되었으면 jump
